import { Link } from "@/shared/ui/link/link";

import { Badge } from "@/shared/ui/badge/badge";

import { Divider } from "@/shared/ui/divider/divider";

export const metadata = {
  title: "Documentation: Installation - SafeHeaders",
  description:
    "In this simple guide, we'll show you how to install our project, so you can later implement a new note.",
};

# What is SafeHeaders CLI?

We’ve developed a **CLI tool** that enables you to **achieve the same results** without having to visit our website.

<div className="flex flex-wrap gap-2">
  <Badge>Windows</Badge>
  <Badge>Ubuntu</Badge>
  <Badge>MacOS</Badge>
  <Badge color="amber">Work in progress</Badge>
</div>

<Divider className="my-5 !border-zinc-800" />

## How to Install the SafeHeaders CLI Tool?

We’ve developed a CLI tool that enables you to achieve the same results without having to visit our website. Here’s how you can install it:

<ol>
<div className="flex flex-col mb-5 gap-5">
1. **Install the CLI Tool**: Use the command below to install the CLI tool globally on your machine:

```sh
sudo apt install safeheaders-cli
```

</div>

<div className="flex flex-col gap-5">
2. **Usage**: After installation, you can start using the CLI tool by running:

```sh
safeheaders-cli --help
```

</div>

</ol>

With the CLI tool installed, you'll be able to perform tasks **more efficiently** without needing to access the website.

<Divider className="my-5 !border-zinc-800" />

## Learn How to Use the CLI

Below is a list of all the attributes and commands you can use with our CLI tool in the terminal. Explore these options to make the most out of the tool and enhance your workflow:

```sh
safeheaders-cli helloworld.com # Check the security headers of a website
```

For a comprehensive guide on each command and its parameters, refer to the [CLI documentation](#).
