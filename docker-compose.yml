version: "3.7"

services:
  postgres:
    image: postgres:12
    container_name: postgres
    ports:
      - "5432:5432"
    env_file:
      - .env.local
    volumes:
      - ./postgres/data:/var/lib/postgresql/data
